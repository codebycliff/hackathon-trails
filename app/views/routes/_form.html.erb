<%= form_for(route) do |f| %>
  <% if route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(route.errors.count, "error") %> prohibited this route from being saved:</h2>

      <ul>
      <% route.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :trail_id %>
    <%= f.text_field :trail_id %>
  </div>

  <div class="field">
    <%= f.label :transit_id %>
    <%= f.text_field :transit_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
